/**
 * @function
 * @name maxmin
 * @param {number} max
 * @param {number} min
 * @returns {string}
 */
export const maxmin = (max, min) => `max-width: ${max}px, min-width: ${min}px`

/**
 * @function
 * @name max
 * @param {number} max
 * @returns {string}
 */
export const max = (max) => `max-width: ${max}px`

/**
 * @function
 * @name min
 * @param {number} min
 * @returns {string}
 */
export const min = (min) => `min-width: ${min}px`
